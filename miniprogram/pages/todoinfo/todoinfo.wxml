<van-cell-group>
  <van-cell 
    title="{{task.title}}"
    border="{{ false }}"
    value="{{task.status==='in-progress'?'进行中':'已完成'}}"
  />
</van-cell-group>
<image wx:if='{{task.image}}' src='{{task.image}}'></image>